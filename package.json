{
  "name": "@repo/root",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev --filter=@repo/api --filter=@repo/db --filter=@repo/types",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "turbo": "^2.1.2"
  },
  "packageManager": "pnpm@8.7.4"
}
